[Unit]
Description=Ara Service
After=network.target

[Service]
Type=simple
User=ara
Group=ara
WorkingDirectory={{ara_home}}
ExecStart={{ara_virtual_env | default("/usr/local")}}/bin/ara-manage runserver

[Install]
WantedBy=multi-user.target
